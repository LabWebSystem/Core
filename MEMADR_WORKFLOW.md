# MEMADR_WORKFLOW.md

## 厳守事項

このファイルを作業開始前に必ず読み、以下を厳守する。

- MemADRは作業ログではなく、将来の開発判断に必要な事実・原因・判断・変更だけを記録する。
- レコード種別は `BUG`、`ADR`、`CHG`、`SUP` の4種だけとする。
- `memory/` 配下のMarkdownを正本とし、`memory/generated/` は手動編集しない。
- `memory/` に試行錯誤や逐次的な作業ログを書かない。
- 古い情報を削除しない。不要になったら `SUP` を作り、元レコードを `ARCHIVED` または `SUPERSEDED` にする。
- 無効化した情報を現在の制約として扱わない。
- 不確かな内容を確定事項として記録しない。
- コード・テストとレコードが矛盾したら、コード・テストを確認してレコードを更新する。
- レコードを追加・更新したら、完了前に必ず `memadr check` と `memadr index` を実行する。

## 作業開始

必ず次を実行し、現在有効な情報を確認する。

```bash
memadr index
memadr list --status OPEN
memadr list --status UNRESOLVED
memadr list --status ACCEPTED
memadr list --status ACTIVE
```

対象領域がある場合は、変更前に検索する。

```bash
memadr search "<機能名または概念>"
memadr related <RECORD-ID>
```

確認対象は、未解決事項、現在有効なADR、対象領域のBUG・CHG、無効化済みの前提とする。

## レコードの使い分け

| 種別 | 記録する内容 |
|---|---|
| `BUG` | 発生した不具合、その原因、修正、検証 |
| `ADR` | 採用した設計判断、却下案、結果 |
| `CHG` | 実施した機能・設計・構成の変更 |
| `SUP` | 過去の判断・変更・BUGを無効化または置換した事実 |

判断に影響しない軽微な作業や一時的な失敗は記録しない。

## 作業完了

- レコードを変更した場合は `memadr check` のエラーを全て解消する。
- `memadr index` で生成物を更新する。
- 実装変更、検証、レコード変更、無効化した過去情報、未解決事項を報告する。
- レコードを作らなかった場合は、その理由を報告する。

## 禁止事項

- `memory/` への作業ログの保存
- `memory/generated/` の手動編集
- `BUG`、`ADR`、`CHG`、`SUP` 以外の種別の作成
- 検証していない修正を `VERIFIED` とすること
- 無効化済みのレコードを新しい基準より優先すること
- 参照関係を確認せずに `ADR` や `SUP` を作成すること

不要になった情報は消去せず、現在の判断対象から外す。
