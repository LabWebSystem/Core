openapi: 3.1.0
info:
  title: LabWebSystem Backend API
  version: 1.0.0
  description: LWSのアプリ管理API
servers:
  - url: /api/v1
paths:
  /health/live:
    get:
      operationId: healthLive
      responses:
        '200': { description: 稼働中 }
  /health/ready:
    get:
      operationId: healthReady
      responses:
        '200': { description: 利用可能 }
        '503': { description: 利用不可 }
  /applications:
    get:
      operationId: listApplications
      responses:
        '200': { description: アプリ一覧 }
    post:
      operationId: createApplication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateApplicationRequest'
      responses:
        '202': { description: Operationを作成 }
  /applications/{application}:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    get: { operationId: getApplication, responses: { '200': { description: アプリ取得 }, '404': { description: 未検出 } } }
    patch:
      operationId: updateApplication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateApplicationRequest'
      responses: { '202': { description: Operationを作成 } }
    delete:
      operationId: unregisterApplication
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ActionRequest' }
      responses: { '202': { description: Operationを作成 } }
  /applications/{application}:start:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    post:
      operationId: startApplication
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ActionRequest' } } } }
      responses: { '202': { description: Operationを作成 } }
  /applications/{application}:register:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    post:
      operationId: registerApplication
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ActionRequest' } } } }
      responses: { '202': { description: Operationを作成 } }
  /applications/{application}:stop:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    post:
      operationId: stopApplication
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ActionRequest' } } } }
      responses: { '202': { description: Operationを作成 } }
  /applications/{application}:sync:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    post:
      operationId: syncApplication
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ActionRequest' } } } }
      responses: { '202': { description: Operationを作成 } }
  /applications/{application}:rebuild:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    post:
      operationId: rebuildApplication
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ActionRequest' } } } }
      responses: { '202': { description: Operationを作成 } }
  /applications/{application}:purge:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    post:
      operationId: purgeApplication
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PurgeRequest' } } } }
      responses: { '202': { description: Operationを作成 } }
  /applications/{application}/configuration:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    get: { operationId: getConfiguration, responses: { '200': { description: 設定取得 } } }
    patch:
      operationId: updateConfiguration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigurationRequest'
      responses: { '202': { description: Operationを作成 } }
  /operations/{operation}:
    parameters: [{ name: operation, in: path, required: true, schema: { type: string } }]
    get: { operationId: getOperation, responses: { '200': { description: Operation取得 } } }
  /operations/{operation}:watch:
    parameters: [{ name: operation, in: path, required: true, schema: { type: string } }]
    get: { operationId: watchOperation, responses: { '200': { description: SSE } } }
  /applications/{application}:tailLogs:
    parameters: [{ name: application, in: path, required: true, schema: { type: string } }]
    get: { operationId: tailLogs, responses: { '200': { description: SSE } } }
components:
  schemas:
    CreateApplicationRequest:
      type: object
      required: [repositoryUrl, ref, subdomain, requestId]
      properties:
        repositoryUrl: { type: string, format: uri }
        ref: { type: string, minLength: 1 }
        subdomain: { type: string, pattern: '^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$' }
        requestId: { type: string, format: uuid }
    UpdateApplicationRequest:
      type: object
      required: [requestId]
      properties:
        ref: { type: string }
        subdomain: { type: string }
        requestId: { type: string, format: uuid }
    ConfigurationRequest:
      type: object
      required: [variables, requestId]
      properties:
        variables: { type: object, additionalProperties: { $ref: '#/components/schemas/Variable' } }
        requestId: { type: string, format: uuid }
    ActionRequest:
      type: object
      required: [requestId]
      properties:
        requestId: { type: string, format: uuid }
    PurgeRequest:
      type: object
      required: [requestId, confirm]
      properties:
        requestId: { type: string, format: uuid }
        confirm: { type: boolean, const: true }
    Variable:
      type: object
      required: [value, secret]
      properties:
        value: { type: string }
        secret: { type: boolean }
    Error:
      type: object
      required: [error]
      properties:
        error: { type: object }
