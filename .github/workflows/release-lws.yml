name: Release LWS
on:
  push:
    tags: ['lws-v*']
permissions:
  contents: write
  packages: write
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
      - run: mise run lint
      - run: mise run test
      - run: LWS_VERSION="${GITHUB_REF_NAME#lws-v}" mise run package
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and publish backend image
        run: |
          VERSION="${GITHUB_REF_NAME#lws-v}"
          docker build --tag "ghcr.io/${GITHUB_REPOSITORY_OWNER}/lws-backend:${VERSION}" backend
          docker push "ghcr.io/${GITHUB_REPOSITORY_OWNER}/lws-backend:${VERSION}"
      - name: Build and publish dashboard image
        run: |
          VERSION="${GITHUB_REF_NAME#lws-v}"
          docker build --tag "ghcr.io/${GITHUB_REPOSITORY_OWNER}/lws-dashboard:${VERSION}" dashboard
          docker push "ghcr.io/${GITHUB_REPOSITORY_OWNER}/lws-dashboard:${VERSION}"
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "$GITHUB_REF_NAME" dist/* --title "LWS ${GITHUB_REF_NAME#lws-v}" --generate-notes
