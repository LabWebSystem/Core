# CHG-028: フェーズ2の入力・実効Compose検証を接続する

Status: SHIPPED
Reason: 静的validatorがあっても、runtimeがdocker compose configを経由せず起動すると、変数展開後の禁止構成を拒否できないため。また全体test targetの実行漏れがあったため。
Change: Compose変数参照と必須値のvalidator、source .env拒否、実効Composeのconfig --format json検証を追加し、up前に検証するようにした。Host境界、標準エラー形式、runtimeの禁止構成拒否テストを追加した。`mise run test`がBackend・HTTP・Docker境界・Infrastructure targetも実行するよう修正した。
FilesChanged: backend/validate.go, backend/source.go, backend/runtime.go, backend/http.go, backend/backend_test.go, scripts/test.sh
Verification: mise run test, mise run lint, mise run check-openapi, docker compose -f infrastructure/compose.yaml config, git diff --check, memadr check, memadr index
Related: ADR-003, CHG-025, CHG-027
CurrentValue: current
ValidityScope: フェーズ2入力検証・テスト入口
