# CHG-058: miseの内部taskをライブラリ設定へ分離する

Status: SHIPPED
Reason: ルートの`mise.toml`に内部テスト、QA、生成用taskが集まり、利用者向けの主要入口を見つけにくかったため。
Change: ルートの`mise.toml`はverify、dev、build、package、version、releaseとツール設定だけを持つ。lint、test、SDK・Dashboard検証、QA、OpenAPI生成は`scripts/mise.lib.toml`へ移し、`[task_config].includes`で読み込む。task名と実行内容は維持する。
FilesChanged: mise.toml, scripts/mise.lib.toml, scripts/test-quality-architecture.sh
Verification: mise tasks validate, mise tasks ls, mise run test architecture, mise run verify fast, mise run lint
Related: ADR-010, CHG-056
CurrentValue: current
ValidityScope: LWSモノレポのmise task定義
